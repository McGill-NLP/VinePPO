{
    global_vars: {
        seed: std.parseInt(std.extVar('APP_SEED')),
        debug_mode: false,
        dirs: {
            experiments: 'experiments',
            data: 'data',
        },
    },
}
